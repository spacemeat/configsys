# apod: splat NASA's Astronomy Picture of the Day (termapod) on interactive shell startup (rc.elv runs
# for interactive sessions). No-op until termapod is installed.
if (has-external termapod) { termapod }
