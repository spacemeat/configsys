'''configsys.v2 — the capability/component routing model (see docs/routing-model.md).

Built in parallel with the current resolver. Nothing here is wired into the app yet;
it exists to be developed and proven equivalent to the old routing before any flip.
'''
